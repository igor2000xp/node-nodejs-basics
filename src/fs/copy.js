import * as fs from 'fs/promises';
import * as path from 'path';

const DESTINATION_PATH = 'files_copy';
const SOURCE_PATH = 'files';

export const copy = async () => {
    // Write your code here
};
